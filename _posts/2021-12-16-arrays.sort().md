---
layout: post
title:  "Arrays.sort()를 사용하여 정렬"
date:   2021-12-16 21:33:00 +0800
categories: Java
mainfont: GowunDodum-Regular
---

Java를 시작한지 얼마 안 된 사람으로써.. 정렬마저 새롭다 ^^,,
오름차순 정렬은 제공되는 `Arrays.sort()` 메서드를 사용하면 되지만, 내림차순 정렬을 한다든지 key-value에서 value로 정렬한다든지 할 때 
이 sort 함수를 수정해주어야한다.

## c++ / python과 비교해보자


## 1단계. sort() 함수로 오름차순 정렬!
Arrays.sort()
```Java
int[] numbers = new int[]{10, 50, 30, 20, 40};
Arrays.sort(numbers);
```

## Comparator 구현을 이용한 배열 정렬

## 람다식을 이용한 배열 정렬

